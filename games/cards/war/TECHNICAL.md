# War - Technical Documentation

## Architecture
*   **Framework**: Shared Card Engine.
*   **Logic**: `WarRuleset` (`ruleset.js`).
*   **UI**: `WarUI` (`index.html`).

## Features
*   **History**: Tracks match history and round outcomes in `localStorage`.
*   **Auto-Shuffle**: Logic to recycle graveyard into deck when empty.
