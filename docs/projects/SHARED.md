# Shared Utilities

**Directory**: `shared/`

## Overview
Global utilities available to all projects.

## Contents
*   **`shared/js/letters-data.js`**: (Currently minimal) Data file likely used for text-based games or logic.

## Future Plans
The **NEGEN** engine will reside here or in a top-level `negen/` directory, providing a unified `Engine`, `Input`, and `Audio` stack for future development.
