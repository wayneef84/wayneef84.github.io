class EmuAdapter {
    constructor() {
        this.isLoaded = false;
    }

    loadROM(romData) {
        console.log("Loading ROM...", romData);
        // Future implementation
    }

    step() {
        // Future Z80 step
    }
}
